{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "user.deleted",
  "description": "DEPRECATED account erasure signal. stapel-gdpr emits it alongside gdpr.erasure.requested for account subjects until its 0.6.0; since 0.8.0 this module consumes both through stapel_core.gdpr.register_gdpr_owner and routes them through one erase call (stapel_video.erasure.erase_subject), so the handler can be deleted without deleting any erasure logic. It receipts gdpr.section.erased only when the payload carried a correlation_id.",
  "type": "object",
  "required": [
    "user_id"
  ],
  "properties": {
    "user_id": {
      "type": "string"
    },
    "correlation_id": {
      "type": "string",
      "format": "uuid"
    },
    "trigger": {
      "type": "string",
      "enum": [
        "manual",
        "inactivity",
        "platform"
      ]
    },
    "deleted_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "additionalProperties": true
}
